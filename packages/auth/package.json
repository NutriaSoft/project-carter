{
	"name": "@package/auth",
	"types": "./src/index.ts",
	"module": "./src/index.ts",
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.6.3"
	},
	"type": "module",
	"dependencies": {
		"@prisma/client": "5.21.1",
		"@types/nodemailer": "^6.4.16",
		"better-auth": "^0.5.2-beta.17",
		"nodemailer": "^6.9.15",
		"prisma": "^5.21.1"
	},
	"scripts": {
		"generate": "bunx @better-auth/cli generate --config ./auth.ts",
		"postinstall": "bunx @better-auth/cli generate && bun prisma format && bun prisma generate"
	}
}
